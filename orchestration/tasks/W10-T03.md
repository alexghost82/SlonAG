# W10-T03 — feature-pairing-dashboard

## Objective

SwiftUI Pairing and Dashboard features: one-time code / QR payload display, paired device list + unpair, and a main panel showing desktop online state, provider/model placeholders, network profile, and assistant start/pause/stop controls (wired to injectable client protocols).

## Context

Starts **after W10-T01 and W10-T02 are integrated**. Import DesignSystem + Networking/Models as dependencies (read-only). Do not edit those packages' sources.

## Base commit

`60107135f7938007502eb5d4fe2e6eeb07588b9f` — integration/main after W10-T01+T02.

## Owned paths

- `ios/MarkRemote/Features/Pairing/**`
- `ios/MarkRemote/Features/Dashboard/**`
- `ios/Tests/MarkRemoteFeaturesTests/PairingDashboardTests.swift`

## Forbidden paths

- `ios/Package.swift`, DesignSystem, Networking, Security, Models source trees
- Other `Features/*` folders
- Python / server code

## Implementation requirements

- PairingView: enter/display one-time code; show `qr_payload` as text (QR rendering optional via system APIs only if no new deps); call injectable `PairingServing`
- Unpair / forget device
- DashboardView: online/offline badge, provider/model text, network mode, runtime placeholder, mic indicator placeholder, start/pause/stop buttons calling injectable controller
- Russian user-visible strings
- No AI API keys. No public desktop discovery beyond injected host list

## Tests

```bash
cd ios
export DEVELOPER_DIR="${DEVELOPER_DIR:-/Users/slon/Downloads/Xcode-beta.app/Contents/Developer}"
swift test --filter PairingDashboardTests
```

View models / services with fakes — not UI snapshots required.

## Stop conditions

- need to edit Package.swift or other Features
- need a QR SPM package
