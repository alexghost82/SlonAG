# W01-T04 — game-updater-guard

## Objective

`actions/game_updater.py` imports on macOS/Linux: `winreg` is optional, and Steam/Epic registry lookup returns `None` when it is unavailable.

## Context

OpenClaw working copy has an uncommitted guard. Port that behavior only. Do not copy any other OpenClaw files.

Reference (read-only): `/Users/slon/.openclaw/workspace/Slon/actions/game_updater.py`

Current integration file does `import winreg` at module level, which fails on non-Windows.

## Base commit

Worktree HEAD after creation from `integration/main` Wave 1 spec commit. Content parent: `6a588b0ce150e4ae5a7641654d2f4bcb21eb2b43`.

## Owned paths

- `actions/game_updater.py`
- `tests/unit/actions/test_game_updater.py`

## Forbidden paths

- all other paths
- other `actions/*` files
- `config/**`, `requirements*.txt`, `tests/conftest.py`, `tests/unit/config/**`, `tests/unit/requirements/**`

## Required interfaces

Keep existing public functions in `game_updater.py` callable. Do not rename the module API.

## Implementation requirements

Port the OpenClaw guard:

```python
try:
    import winreg
except ImportError:
    winreg = None
```

In `_find_steam_path` and `_find_epic_path`, if `winreg is None`, return `None` before using registry APIs.

Do not refactor the rest of the file.
Do not reformat the whole file.
Do not add new features (macOS Steam support, etc.).
Do not copy secrets or unrelated OpenClaw edits.

## Tests

`tests/unit/actions/test_game_updater.py`:

- importing `actions.game_updater` succeeds on this host
- when `winreg` is `None`, `_find_steam_path()` and `_find_epic_path()` return `None`
- do not launch Steam/Epic or hit the network

If the module import pulls heavy Windows-only deps at top level besides `winreg`, isolate the test to the guard logic (importlib or a focused unit). If a missing optional import outside `winreg` blocks import, stop and file a change request rather than adding new dependencies.

Do not edit `tests/conftest.py`.

Run:

```bash
python -m pytest tests/unit/actions/test_game_updater.py -q
```

## Acceptance criteria

- Module imports on macOS.
- Registry helpers are safe without `winreg`.
- No unrelated edits.
- Tests pass.
- One commit.

## Deliverables

- patched file, test, one commit, report

## Stop conditions

- need to edit other action modules or requirements
- discovered secret in the OpenClaw file
- need a large rewrite to make the module import
