# W07-T05 — reminder-safe

## Objective

Fix `actions/reminder.py`: no generated Python from user text, JSON payload, XML via library, no `shell=True`, list/update/cancel, macOS/Linux backends.

## Context

Starts **after W07-T01**. Keep `reminder(parameters=..., player=None)` as the executor entry.

## Base commit

`d2e309adf389c71989d57633ea1d85c7e904acd0` — integration/main after W07-T01.

## Owned paths

- `actions/reminder.py`
- `tests/unit/actions/test_reminder.py`

## Forbidden paths

- `agent/**`, `actions/file_controller.py`, `actions/desktop.py`, `ui.py`
- `mark/safety/**` (read-only import ok)

## Implementation requirements

- Do not interpolate user `message` into a `.py`/`.pyw` script
- Store reminder payload as JSON (injected path in tests)
- Build Windows task XML with `xml.etree.ElementTree` (or equivalent), not an f-string of raw user text inside a shell command
- `subprocess` only with `shell=False` and an argv list; prefer an injected scheduler
- Backends: `windows` | `macos` | `linux` selected by injected OS (tests fake all three)
- Operations: create, list, update, cancel
- `authorize` before create/update/cancel
- No `shell=True` anywhere in this file

## Tests

- user message with quotes/`; rm` does not appear in a generated Python file
- no `shell=True` in source
- list/update/cancel work against the JSON store
- macOS and Linux backends are invoked via injection (no real `osascript`/`at` required)

```bash
python -m pytest tests/unit/actions/test_reminder.py -q
```

## Stop conditions

- need `shell=True`
- need to edit executor
