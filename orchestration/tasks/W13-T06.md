# W13-T06 — mypy-cleanup

## Objective

Reduce pre-existing mypy debt meaningfully without weakening `pyproject.toml`
mypy strictness. Prefer fixing real types in the new stack (`mark/`,
`providers/`, `speech/`, `server/`, `policies/`, `localization/`, `config/`) and
hot test paths. Report remaining error count.

## Owned paths

- Type fixes under: `mark/**`, `providers/**`, `speech/**`, `server/**`,
  `policies/**`, `localization/**`, `config/**` (as needed)
- Matching test annotation fixes under `tests/unit/**` for those packages
- `docs/audit/mypy-wave13.md` (before/after counts, command used)
- Optional: `pyproject.toml` **only** to add packages to `files` / `mypy_path`
  or install stubs — **do not** add `ignore_errors = True` globally or disable
  useful checks to "pass"

## Forbidden paths

- Disabling mypy broadly; deleting tests to hide errors
- Unrelated refactors

## Acceptance

- Document baseline vs after (tool: mypy on Python 3.12; note 3.14 venv crash).
- Meaningful reduction (target: fix new-stack + high-count test modules first).
- `ruff` still clean on tests scope; pytest green.
- One commit.

## Depends on

Prefer after T01–T05 code lands so new files are typed too.
